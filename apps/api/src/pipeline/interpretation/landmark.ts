import type { MediaInterpretation, MediaReference } from '@rabbit-hole/contracts';

/**
 * Landmark / geology identification.
 * Stubbed — interface defined; logic not yet implemented.
 */
export function interpretLandmark(_media: MediaReference): MediaInterpretation | null {
  return null;
}
